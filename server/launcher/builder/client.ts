export function buildClientSideScript(): Promise<void> {
   // client side import modules
   // fullstack common context
   // client-side render
   // SSG streaming?
   // client-route?


   return Promise.resolve()
}